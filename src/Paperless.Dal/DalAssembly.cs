namespace Paperless.Dal;

/// Platzhalter/Marker fuer den Datenzugriff; die Implementierung uebernimmt die DAL-Partnerin.
/// Plan fuer Sprint 1: DbContext und PostgreSQL-Entity-Konfiguration hinzufuegen,
/// IDocumentRepository implementieren und Schreiboperationen dauerhaft speichern.
/// Dokumentnotizen ueber DocumentId verknuepfen und beim Loeschen des Dokuments mitloeschen.
/// Danach DbContext und Repository in der API registrieren und Persistenz gemeinsam pruefen.
/// Binaere Dateien im Object Storage gehoeren in einen spaeteren Sprint, nicht in diese Klasse.
public static class DalAssembly
{
}
